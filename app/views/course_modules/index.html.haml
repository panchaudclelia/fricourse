.page-header
  = link_to new_course_module_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Course module
  %h1 Listing course_modules

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Name
        %th
        %th
        %th

    %tbody
      - @course_modules.each do |course_module|
        %tr
          %td= course_module.name
          %td= link_to 'Show', course_module
          %td= link_to 'Edit', edit_course_module_path(course_module)
          %td= link_to 'Destroy', course_module, data: { confirm: 'Are you sure?' }, method: :delete
